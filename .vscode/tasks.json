{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: make file",
            "command": "rm -rf build && mkdir build && clang -g -Wall -I /usr/local/include -framework OpenGL ./libs/glad/src/*.c /usr/local/lib/libglfw.3.3.dylib ./src/*.c -o ./build/main",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build"
        },
        {
            "type": "shell",
            "label": "C/C++: build & run",
            "command": "rm -rf build && mkdir build && clang -g -Wall -I /usr/local/include -framework OpenGL ./libs/glad/src/*.c /usr/local/lib/libglfw.3.3.dylib ./src/*.c -o ./build/main",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": []
        }
    ]
}